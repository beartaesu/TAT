# 02 TAT CURRENT STATUS — 현재 구현 및 검증 상태

최종 갱신: 2026-08-20

## 문서 역할과 갱신 기준

- 역할: 현재 구현·검증 상태와 다음 작업을 보여주는 작업대장
- 포함: 완료·진행·보류 상태, 열린 오류, 최근 테스트 결과, 현재 재개 위치와 우선순위
- 제외: 장기 설계의 반복 설명, 전체 수식 원문, 확정 결정의 장문 근거
- 갱신: 구현 상태, 테스트 성공·실패, 오류 상태, 다음 작업 또는 재개 위치가 바뀔 때
- 완료된 과거 기록은 필요한 요약만 남기고, 실행 가능한 수식은 04를 참조한다.

## 1. 현재 도달 상태

접수 입력·항목 선택·시료별 배정·검토·저장, 접수현황 조회, 접수상세 진입, 접수상태 변경, 미접수↔측정인 구분변경의 주요 기능이 구현되어 있다. `TAT_결과파일생성_App.Run()`의 15개 인수 적용과 신규 결과 폴더·Excel 생성을 검증했다. 구분변경 Flow의 응답 4개, Flow 내부 접수헤더·변경이력 갱신, 폴더 이동 작업의 고정 간격 재시도 정책을 적용했고 실패 실행 재제출 복구까지 검증했다.

## 1.1 2026-08-12 분석진행 위임 안정화 — 헤더 ID 복사 열

- 사용자 확인: SharePoint `접수항목상세`에 숫자 열 `접수헤더ID복사`를 생성하고 해당 열의 인덱스를 만들었다.
- 저장소 반영: `scr검토저장 > btn최종저장 > OnSelect`의 상세 생성 분기 3곳에서 `Round(Value(var저장헤더.ID), 0)`을 `접수헤더ID복사`에 저장하도록 변경했다.
- 저장소 반영: 기존 상세를 교체하는 분기 2곳의 `RemoveIf` 조건을 `접수헤더연결.Id` 비교에서 인덱싱된 `접수헤더ID복사` 숫자 비교로 변경했다.
- Power Apps 적용 확인: 전체 `btn최종저장.OnSelect` 수식 적용 후 문법 오류와 Flow 인수 오류가 없었다. 남은 대규모 데이터 경고 4개는 모두 변경 대상이 아닌 `접수시료항목매핑`의 `접수헤더연결.Id = var저장헤더.ID`에 있으며, `접수항목상세.접수헤더ID복사` 조건에는 경고가 없다.
- 신규 저장 검증 완료: `ES-DR-2026-027`(접수헤더 ID 36)을 저장했고 Power Apps 성공, 상세 행 ID 98 생성, `접수헤더연결` 정상, `접수헤더ID복사 = 36`, 기존 복사 열 5개 정상, 시료항목매핑 Lookup 정상, 결과 폴더·Excel 생성을 모두 확인했다. 오류와 특이사항은 없었다.
- 테스트 데이터 정리 완료: 사용자가 `접수시료항목매핑`, `접수항목상세`, `접수헤더`의 기존 행과 테스트 결과 폴더를 모두 삭제했고 `접수번호관리`는 유지했다. 따라서 실패 행 ID 82·83을 포함한 기존 상세 보정은 불필요하다.
- 상태: **신규 저장 및 `접수항목상세` 직접 숫자 ID 방식 검증 완료, 기존 테스트 데이터 정리 완료**.
- 다음 재개 위치: `접수시료항목매핑`에 숫자 열 `접수헤더ID복사`를 생성·인덱싱한 뒤 매핑 생성 4개 분기와 `RemoveIf` 4곳을 직접 숫자 열 방식으로 전환한다.
- 사용자 확인: `접수시료항목매핑.접수헤더ID복사` 숫자 열과 인덱스 생성을 완료했다.
- 저장소 교정: 매핑 생성 분기 4곳에 `Round(Value(var저장헤더.ID), 0)` 저장을 추가하고, 매핑 삭제 조건 4곳을 인덱싱된 `접수헤더ID복사` 숫자 비교로 전환했다.
- Power Apps 적용 확인: 사용자가 전체 `btn최종저장.OnSelect`를 적용했고 수식 오류, Flow 인수 오류 및 대규모 데이터 경고가 모두 없음을 확인했다.
- 신규 저장 실제 검증 완료: `ENE-260813-019`(접수헤더 ID 37)을 저장했고 Power Apps 성공, 상세 ID 99의 Lookup·`접수헤더ID복사 = 37`·복사 열 5개·중복 없음, 매핑 ID 56의 Lookup·`접수헤더ID복사 = 37`·시료순번 1/BOD·중복 없음을 확인했다. 결과 폴더·Excel도 정상 생성됐고 오류·특이사항은 없었다.
- 상태: **상세·매핑 직접 숫자 ID 방식 신규 저장 검증 완료**.
- 다음 재개 위치: `scr분석진행 > gal분석진행항목.Items`와 시료명요약 표시를 `접수항목상세` 복사 열 직접 참조 방식으로 교체하고 필터 단독·조합, 전체선택 및 분석완료 처리 범위를 재검증한다.
- 사용자 검증 완료: `gal분석진행항목.Items`를 `업체현장ID복사`, `의뢰일자복사`, `분석항목ID복사` 직접 필터 방식으로 교체하고 `lbl분석진행시료명요약.Text`를 `ThisItem.시료명요약복사` 직접 표시 방식으로 교체했다. 신규 건과 시료명요약이 정상 표시되고 오류·경고가 없다.
- 사용자 검증 완료: 초기 전체 표시, 업체/현장·월·분석항목의 단독 일치·불일치, 세 필터 조합 일치·불일치, 초기화 후 복원, 필터 밖 선택 행의 선택 해제·건수·전체선택 동기화가 모두 정상이고 오류·대규모 데이터 경고가 없다.
- 분석완료 처리 최종 검증 완료: 개별선택, 완료일 지정, 재처리 시 완료일 갱신, 현재 조회 전체선택, 처리 후 선택 컬렉션·건수·전체선택 초기화, 선택 행의 상태·완료일 반영, 비대상 열 유지와 중복 미생성이 모두 정상 작동했다.
- 상태: **`scr분석진행` 조회·복사 열 필터·개별/전체선택·분석완료 처리 기능 검증 완료**.
- 다음 재개 위치: 보류했던 구분변경 안정화를 재개한다. 실제 트리의 정확한 위치는 `scr접수상세 > con구분변경패널 > con구분변경버튼행 > btn구분변경실행.OnSelect`이다. 최신 전체 원문을 확보했으며 현재 Flow 변경 분기 구조를 확인한 뒤 응답 수식 교정 → Flow 내부 접수헤더·변경이력 갱신 → Excel 잠금 조건부 재시도 순으로 진행한다.
- Flow 응답식 적용 완료: `변경결과PowerApps응답`의 결과폴더명·결과폴더경로·결과엑셀파일명·결과엑셀파일경로 4개를 `변경후폴더서버경로` 기준 표현식으로 교체하고 Flow 저장을 완료했다. 실제 구분변경 실행 검증은 아직 하지 않았다.
- Flow 내부 헤더 갱신 구성 완료: `변경접수번호B4반영` 뒤에 `변경접수헤더조회`를 추가하고 `접수헤더ID`로 현재 행을 조회했다. `변경접수헤더처리완료`의 제목·의뢰일자·시료수와 추가 필수 열은 조회 동적 콘텐츠로 보존하고, 변경 후 경로 4개·처리완료·오류 공란·최종처리일시를 설정해 저장했다. 오류는 없다.
- Flow 내부 변경이력 갱신 구성 완료: `변경이력조회`와 `변경이력처리완료`가 이미 추가되어 있으며 둘 다 Power Apps (V2)의 `변경이력ID`를 사용한다. 현재 성공 경로는 헤더 조회·처리완료 → 이력 조회·처리완료 → Power Apps 응답 순으로 구성됐다.
- 검증 진행: 성공 경로 내부 갱신 완성 후 새 테스트 건으로 실제 폴더·Excel, 접수헤더 경로 4개·상태, 변경이력 처리결과와 응답값 검증을 시작했다.
- 2026-08-13 실제 테스트 실패: `ES-DR-2026-028 → ENE-260813-020` 구분변경에서 `기존폴더이동및이름변경`이 실패했다. SharePoint 응답은 기존 Excel이 `rnjx@eurofinsasia.com`에 의해 shared use로 잠겼다는 `SPException(-2147018894)`이며, 폴더 이동 HTTP POST 단계에서 중단됐다.
- 금지: 같은 건의 Power Apps 구분변경 버튼 재실행 금지. 접수번호와 변경이력이 이미 생성됐을 수 있으므로 현재 실패 실행을 기준으로 복구해야 한다.
- 복구 검증 완료: Flow 저장 후 실패 실행을 `테스트 > 자동 > 최근 실패`로 다시 제출했고 `기존폴더이동및이름변경`부터 후속 작업까지 모두 성공했다. 이전 폴더는 제거되고 변경 후 폴더·Excel이 생성됐으며 Excel B4, 접수헤더 경로 4개·처리상태·오류 공란, 변경이력 처리결과가 모두 정상이다.
- 재시도 정책: `기존폴더이동및이름변경`에 고정 간격 2분, 최대 3회를 적용했다. 이번 재제출에서는 잠금이 이미 해제되어 자동 재시도는 발생하지 않았으므로 실제 잠금 지속 중 재시도 횟수 관찰만 남아 있다.
- 2026-08-18 실제 트리 재확인: `scr접수상세 > con구분변경패널` 아래 컨테이너명은 기존 문서의 `con구분변경상단`이 아니라 `con구분변경상`이다. 실행 버튼의 정확한 위치는 `con구분변경버튼행 > btn구분변경실행.OnSelect`이며 같은 행에 `btn구분변경취소`가 있다.
- 사용자 제공 최신 `btn구분변경실행.OnSelect` 전체 수식을 `formulas/scr접수상세_btn구분변경실행_OnSelect.powerfx`에 보존했다. 실제 Power Apps 적용본에는 Markdown 강조 문자나 이스케이프된 밑줄 오류가 없었고, Power Apps에서 다시 복사해 대화에 붙이는 과정에서 표시가 변형된 것으로 확인했다. 이후 재제공된 `\\n` 표기는 줄바꿈이 문자열로 직렬화된 전송 형태이며 보존 파일은 이를 실제 줄바꿈으로 기록한다.
- 사용자 재확인: URL에서 복사해 Power Apps에 적용한 당시 수식에는 `*` 또는 `\\` 오류가 없었다. 최신 재제공본의 `\\n`을 실제 줄바꿈으로 복원한 내용과 저장소 기준 파일의 논리가 일치하므로 수식 보존·대조 작업은 완료했다.
- 현재 재개 위치: 예정된 구현·복구 작업은 완료 상태이며, 실제 잠금 지속 중 자동 재시도 횟수만 향후 구분변경 실행에서 관찰한다.
- 운영 인계: `origin` 없는 Codex Cloud 체크아웃의 파일은 회사 PC에서 직접 가져오도록 안내하지 않는다. 회사 PC에는 대화에서 전체 수식을 제공하고, 검증 후 개인 PC Local Codex가 정본 저장소에 반영·커밋·push한다. GitHub Raw 사용은 push 완료 후에만 가능하다.
- 2026-08-18 결과 발송 상태 관리 구현·검증 완료: `scr접수상세` 헤더의 `btn결과발송변경`과 `con결과발송패널`을 추가하고 `접수헤더.결과발송여부` 예/아니요 열을 `미발송 ↔ 발송완료` 양방향으로 변경했다. 패널 열기, 취소, 저장, SharePoint 반영과 레이블·버튼 자동 레이아웃 보정까지 정상 완료했다.
- 2026-08-18 접수내용 수정 확인 기능 구현·검증 완료: 기존 `btn접수내용수정`은 `con접수내용수정확인패널`만 열고, `btn접수내용수정진행`이 기존 데이터 조회·수정 모드 설정·`scr접수기본정보` 이동 전체 로직을 실행한다. 취소 시 상세 화면을 유지하며 안내 레이블과 버튼 영역 자동 레이아웃 보정도 완료했다.
- 2026-08-18 정산 상태 관리 구현·검증 완료: `접수헤더.정산상태`의 `정산대기 / 정산확정 / 정산보류 / 정산제외`를 상세 화면에서 양방향 변경한다. `정산제외` 선택 시 `세금계산서배정상태 = 정산제외`, 제외 해제 시 `미배정` 복원을 포함한 네 전환, SharePoint 실제 반영, 패널 열기·취소·레이아웃·알림과 수식 오류 없음까지 전부 확인했다.
- 현재 재개 위치: 결과 발송, 접수내용 수정 확인, 정산 상태 관리 기능 완료. 다음 신규 업무 기능을 진행한다.
- 2026-08-19 `세금계산서발행대장` 신규 생성 완료: 계산서업체 Lookup, 발행일자·상태, 금액 5종, 발행완료일시·비고, 단일 사용자 `발행담당자`, 알림 완료 여부·일시 열을 확인했다. 승인번호·발행일련번호는 제외했다.
- 업무 확인: 한 계산서에 여러 접수 합산, 한 접수의 여러 계산서 분할 발행이 가능하며 단가는 공급가액이다. 할인금액은 접수별 직접 입력, 부가세는 10% 고정·원 단위 반올림을 사용한다.
- 알림 정책: 발행담당자는 항상 사용자 본인이며, 미래 발행일을 지정한 `작성중` 건은 해당 발행일 오전 11시에 담당자 Teams 개인 메시지로 한 번 알린다.
- 2026-08-19 외부 계산서 담당자 구조 확인·열 추가 완료: 기존 `거래처담당자`의 현장·계산서업체 Lookup, 연락처·이메일·담당부서, `담당역할 = 의뢰 / 세금계산서`, 사용여부를 재사용하고 `기본담당자여부`를 추가했다.
- `세금계산서발행대장`에 `계산서담당자연결` Lookup과 담당자명·부서·연락처·이메일 복사 열을 추가했다. 내부 `발행담당자`와 외부 `계산서담당자연결`은 별도 역할로 유지한다.
- 2026-08-19 `세금계산서접수배정` 생성 완료: 세금계산서·접수 Lookup, 직접 숫자 ID 복사 열과 인덱스, 번호 복사, 기준·할인·발행 공급가액, `적용 / 취소`, 배정일시·배정자·할인사유 열을 확인했다.
- `접수헤더` 금액 누계 최종 열 5개를 생성했고, 초기 제안에서 중복 생성된 `세금계산서배정금액`, `세금계산서미배정금액`은 사용자 확인 후 삭제했다.
- 현재 재개 위치: 접수별 공급가액 원천을 `col선택항목.입력수량 × 입력단가`로 확인했다. **제안/미적용:** 핵심 최종저장 수식의 즉시 대형 교체를 피하고 세금계산서 작성 화면에서 선택 접수의 상세를 합산·검증한 뒤 접수헤더 누계와 배정상태를 갱신하는 구현을 시작한다.
- 2026-08-19 `scr세금계산서관리` 빈 화면 생성 및 데이터 연결 검증 완료: `접수헤더`, `세금계산서발행대장`, `세금계산서접수배정`, `거래처담당자`의 필요한 신규 열을 `ShowColumns(Filter(..., ID = -1), ...)`로 정적 참조했고 오류·경고 없이 성공 알림을 확인했다.
- `scr세금계산서관리` 전체 자동 레이아웃 구성 완료: 헤더·필터·본문, 본문 내부 목록·상세 영역이 겹침 없이 정상 표시되고 목록·상세 비율도 적용됐다.
- 헤더와 필터 UI 적용 완료: 뒤로가기·제목·비활성 신규 버튼, 발행상태와 현재 월 발행일 기간·초기화 컨트롤이 정상 표시된다.
- 왼쪽 계산서 목록 제목·조회 건수·갤러리 구성 완료. 기존 앱의 `lbl계산서업체`와 이름 충돌을 피하려고 신규 레이블을 `lbl세금계산서목록업체`로 확정했다.
- `gal세금계산서목록.Items`의 `SortByColumns` 인수 오류를 직접 열 기준 `Sort`로 교정했고 오류·경고 없음, 빈 목록 `조회 0건` 표시를 확인했다.
- 오른쪽 상세 영역 기본 레이아웃 완료: 상세 헤더 48px, 기본정보 220px, 남은 높이의 접수 배정 영역, 하단 56px 버튼행이 겹침·불필요한 스크롤 없이 정상 표시된다.
- 상세 헤더 제목·상태 레이블과 기본정보 내부 3개 가로 입력 행 구성 완료. 선택된 계산서가 없는 상태에서 `세금계산서 상세` 제목, 공백 상태와 동일 높이 3개 행이 정상 표시된다.
- 기본행1 계산서업체·외부 담당자 선택 구현 완료: 레이블 폭을 110px로 맞췄고 업체 변경 시 담당자를 Reset한다.
- `거래처담당자.계산서업체.Id` 위임 경고를 없애기 위해 인덱싱된 숫자 열 `계산서업체ID복사`를 생성·보정하고 담당자 필터를 직접 숫자 비교로 전환했다.
- `cmb세금계산서외부담당자.DefaultSelectedItems`에서 자기 `Items`를 참조해 발생한 순환 참조 오류를 제거하고 `거래처담당자` 원본 직접 필터로 교정했다. 오류·경고 제거와 업체별 담당자·기본담당자 선택을 사용자 확인했다.
- 현재 재개 위치: 기본행2에 발행일자와 내부 발행담당자 표시를 추가한다.
## 2. 2026-08-05 확인 및 변경

- `btn구분변경실행.OnSelect`를 결과 폴더·Excel 자동 변경 Flow 호출형으로 변경했다.
- 존재하지 않는 `var상세접수.현장명` 참조는 기존 `결과폴더명`에서 이전 접수번호를 제거해 현장명을 전달하도록 수정했고 문법 오류가 사라졌다.
- Flow 첫 번째 입력은 `Round(Value(var상세접수.ID), 0)`, 나머지 입력은 `Text(...)`로 형식을 명시했다.
- 앱 재시작 후 수식 오류가 사라졌고 새 접수번호로 테스트했다.
- 신규 최종저장 테스트에서는 접수헤더와 분석항목은 저장됐으나 결과 폴더·Excel은 생성되지 않았고 Flow 실행 기록도 없었다.
- Power Apps의 Flow 연결을 새로고침하자 `15개의 인수가 필요한데 10개를 받았습니다` 오류가 표시됐다.
- 따라서 자동화가 호출되지 않은 직접 원인은 현재 최종저장 수식의 인수 부족으로 확정했다.
- Power Apps (V2) 트리거의 입력값 15개 이름·형식·순서를 확인하고 최종저장 수식의 Flow 호출부 4곳을 모두 최신 인수 구조로 수정했다.
- 수정 후 `15개의 인수가 필요한데 10개를 받았습니다` 오류와 빨간 문법 오류가 사라진 것을 확인했다.
- 새 테스트 접수의 최종저장을 실행했고 결과 폴더·Excel 생성과 SharePoint 접수헤더 연결이 정상 완료됐다.
- 신규 저장의 15개 인수 적용 및 결과파일 생성 검증은 완료됐다.
- `btn구분변경실행.OnSelect`에도 기존 10개 인수 호출이 남아 있어 `15개의 인수가 필요한데 10개를 받았습니다` 오류가 발생했다.
- 첨부된 전체 수식의 Flow 호출부 1곳을 변경 모드용 15개 인수 구조로 수정했다. 아직 Power Apps 문법 및 실제 구분변경 실행은 미검증이다.
- 운영 규칙: 작업 결과는 관련 MD에 누적하고, `AGENTS.md`의 세션 단위 체크포인트에서 관련 변경을 묶어 `main`에 반영한다.
- 구분변경 실제 실행 결과: 성공 알림과 `접수구분변경이력` 생성, 접수헤더의 접수번호·의뢰구분 변경은 확인됐다.
- 같은 실행에서 `scr접수상세`가 백지로 표시됐고, 접수헤더의 결과폴더명·결과폴더경로·결과엑셀파일명·결과엑셀파일경로와 실제 폴더·Excel은 변경되지 않았다.
- 같은 건 재실행은 중복 번호·이력 생성 위험 때문에 금지한다. 다음 진단 자료는 해당 Flow 실행 기록의 전체 단계 및 변경 분기 입력·출력이다.
- 새 테스트 `ES-DR-2026-025 → ENE-260805-016`의 첫 변경 실행은 `기존폴더이동및이름변경`에서 실패했다.
- 오류 원인은 사용자 열람이 아니라 신규 생성 Flow의 Excel Online/Office Script 연결이 남긴 일시 잠금으로 확인됐다. 오류에는 연결 계정 `rnjx@eurofinsasia.com`이 잠금 사용자로 표시됐다.
- 충분히 기다린 뒤 실패 실행 기록을 `다시 제출`하자 실제 폴더가 `측정인`으로 이동하고 폴더명과 Excel 파일명이 `ENE-260805-016_(주)동남합성 대산1공장` 기준으로 정상 변경됐다.
- 접수헤더의 결과폴더명·결과폴더경로·결과엑셀파일명·결과엑셀파일경로는 기존 `ES-DR-2026-025` 값으로 남았다.
- 원인은 재제출 실행의 Flow 응답을 받아 Patch할 Power Apps 실행이 없기 때문이다. 현재 구조는 파일 변경은 Flow, 접수헤더 경로 4개 갱신은 Power Apps 응답 후 Patch로 분리되어 있어 재제출 복구에 취약하다.
- Flow 응답 출력에서 결과폴더경로와 결과엑셀파일경로는 변경 후 경로였으나, 결과폴더명과 결과엑셀파일명에는 `ENE-260805-016_ES-DR-2026-025_...`처럼 변경 전 접수번호가 불필요하게 포함됐다.
- 위 문제는 이번 단계에서 즉시 개편하지 않고 향후 안정화 항목으로 등록했다.
- 다음 기능 작업으로 `scr접수현황` 접수상태 필터를 구현했다.
- `con접수현황헤더`에 `dd접수상태필터`를 추가하고 `전체 / 임시작성 / 접수완료 / 접수취소` 선택지를 적용했다.
- `gal접수현황.Items`를 `dd접수상태필터.Selected.Value` 기준으로 필터링하되 `전체` 선택 시 모든 접수헤더를 표시하도록 교체했다.
- 사용자 테스트 결과 `전체`, `임시작성`, `접수완료`, `접수취소`가 모두 정상 동작했다.
- 다음 작업으로 `scr접수현황` 접수번호 검색 필터를 구현했다.
- `con접수현황헤더`에 `txt접수현황검색`을 추가하고 `gal접수현황.Items`에 검색어가 비어 있으면 상태 필터 결과 전체를 표시하고, 검색어가 있으면 `제목`에 포함된 접수번호를 검색하는 조건을 추가했다.
- 사용자 테스트 결과 검색창 빈 상태, `전체`, `임시작성`, `접수완료`, `접수취소` 상태 필터와 접수번호 검색 조합, 상세보기 이동이 모두 정상 동작했다.
- 다음 작업으로 `scr접수현황` 필터·검색 초기화 버튼을 구현했다.
- `con접수현황헤더`에 `btn접수현황필터초기화`를 추가하고 `Reset(dd접수상태필터); Reset(txt접수현황검색)`으로 상태 필터와 검색어를 기본값으로 되돌리도록 설정했다.
- 사용자 테스트 결과 상태 필터 초기화, 검색어 초기화, 상태+검색 동시 초기화, 초기화 후 전체 목록 표시, 상세보기 이동이 모두 정상 동작했다.
- 다음 작업으로 `scr접수현황` 현재 조회 건수 표시를 구현했다.
- `con접수현황헤더`에 `lbl접수현황조회건수`를 추가하고 `CountRows(gal접수현황.AllItems)` 기준으로 현재 표시 중인 접수 건수를 보여주도록 설정했다.
- 사용자 테스트 결과 기본 조회 건수 표시, 상태 필터별 건수 변경, 검색어 적용 시 건수 변경, 초기화 후 전체 건수 복원, 상세보기 이동이 모두 정상 동작했다.

## 3. 2026-08-04 확인 완료

### scr접수현황

- 목록과 정렬 표시 정상
- 행의 상세보기 선택 시 `scr접수상세` 이동 정상
- 제목은 `접수현황`으로 정리
- `lbl접수현황빈안내.Text`는 다음 수식으로 확정

```powerfx
If(
    CountRows(gal접수현황.AllItems) = 0,
    "현재 조회할 접수가 없습니다.",
    "조회하거나 수정할 접수를 선택하세요."
)
```

2026-08-05 `scr접수현황` 접수상태 필터 구현 및 사용자 검증 완료:

- 필터 컨트롤: `con접수현황헤더 > dd접수상태필터`
- 적용 선택지: `전체`, `임시작성`, `접수완료`, `접수취소`
- `전체`: 모든 접수헤더 표시
- 각 상태값 선택: 해당 `접수상태.Value`인 접수만 표시
- 사용자 테스트 결과 네 선택지 모두 정상
- `분석 중`, `분석완료`는 실제 SharePoint Choice 값 확인 전까지 필터에 추가하지 않음

2026-08-05 `scr접수현황` 접수번호 검색 필터 구현 및 사용자 검증 완료:

- 검색 컨트롤: `con접수현황헤더 > txt접수현황검색`
- 검색 대상: `접수헤더.제목`
- 검색창이 비어 있으면 기존 접수상태 필터 결과를 그대로 표시
- 검색어가 있으면 접수상태 필터 조건과 `제목` 포함 검색 조건을 함께 적용
- 사용자 테스트 결과 검색창 빈 상태, 상태별 검색, 상세보기 이동 모두 정상
- 업체/현장명, 시료명 요약 검색은 실제 열명·Lookup 표시 구조 확인 전까지 확장하지 않음

2026-08-05 `scr접수현황` 필터·검색 초기화 버튼 구현 및 사용자 검증 완료:

- 초기화 컨트롤: `con접수현황헤더 > btn접수현황필터초기화`
- 동작: `dd접수상태필터`와 `txt접수현황검색`을 각각 기본값으로 Reset
- 기본 상태: 접수상태 필터 `전체`, 검색어 빈 값
- 사용자 테스트 결과 상태 필터 초기화, 검색어 초기화, 상태+검색 동시 초기화, 초기화 후 전체 목록 표시, 상세보기 이동 모두 정상

2026-08-05 `scr접수현황` 현재 조회 건수 표시 구현 및 사용자 검증 완료:

- 조회 건수 컨트롤: `con접수현황헤더 > lbl접수현황조회건수`
- 동작: `CountRows(gal접수현황.AllItems)` 기준으로 현재 화면에 표시 중인 접수 건수 표시
- 표시 형식: `조회 N건`
- 사용자 테스트 결과 기본 조회 건수 표시, 상태 필터별 건수 변경, 검색어 적용 시 건수 변경, 초기화 후 전체 건수 복원, 상세보기 이동 모두 정상

### scr접수상세

- 선택 접수의 기본정보 표시 정상
- 분석항목 및 BOD 상세 표시 정상
- 기능별 패널이 정상적으로 열림
- 트리 구조는 기능 검증 전까지 합치거나 삭제하지 않음

### 접수상태 변경

- 상태변경 패널 열림 정상
- 현재 상태 표시 정상
- `수정저장` 실행 정상
- 저장 후 패널 닫힘 정상
- 화면의 접수상태 즉시 갱신
- SharePoint `접수헤더`에도 정상 반영
- 화면에서 확인된 선택지는 `임시작성 / 접수완료 / 접수취소`

### 의뢰구분 변경 — 2026-08-04 당시 수동 처리 설계 기록

테스트 결과:

| 항목 | 결과 |
|---|---|
| 패널 열기/닫기 | 정상 |
| 측정인 → 미접수 | 정상 |
| 새 접수번호 발급 | 정상 |
| 접수헤더 번호·구분 변경 | 정상 |
| 접수항목상세 접수번호 변경 | 정상 |
| 접수시료항목매핑 Lookup | 기존 헤더에 정상 연결 |
| 접수시료항목매핑 제목 | 기존 번호가 남아 수정 필요 확인 |
| 접수구분변경이력 | 정상 생성 |
| 접수헤더 폴더처리상태 | `수동처리대기` |
| NAS 폴더·Excel 파일명 | 자동화 대상 아님, 수동 처리 |

수정 수식은 04 문서의 `btn구분변경실행.OnSelect` 전체본으로 확정했다. 아직 변경 후 재테스트 결과는 확인되지 않았다.

## 4. 기존 구현 완료 항목

- 접수번호 발급 Flow
- 접수헤더 저장
- 접수항목상세 저장
- 시료별 분석항목 매핑
- 상태 변경 패널
- 상세 항목 개별 수정
- 전체선택·선택삭제·일괄 상태변경
- 접수취소 Choice 추가
- 미접수↔측정인 변경
- 변경 전·후 접수번호와 변경이력 보존
- 결과 Excel B3 업체명, B4 의뢰번호 기록 테스트

## 5. 알려진 오류·주의사항

### 2026-08-05 Flow 15개 인수 불일치 — 해결 완료

- Power Apps에서 `TAT_결과파일생성_App` 연결 새로고침 후 발생
- 오류: 15개 인수가 필요하지만 기존 호출부는 10개만 전달
- 신규 접수 최종저장 Flow 실행 기록 없음
- 현재 최종저장 수식에 Flow 호출부가 여러 곳이면 모든 호출을 동일한 15개 시그니처로 맞춰야 함
- Power Apps (V2)의 15개 입력 정의를 확인하고 Flow 호출부 4곳을 모두 교체해 해결
- 수정 후 인수·문법 오류 없음
- 새 테스트 접수에서 결과 폴더·Excel 생성과 SharePoint 연결 정상 확인

### 결과파일 생성 Flow 장시간 대기

`경로를 사용하여 파일 메타데이터 가져오기` 단계 입력에서 다음이 확인됨.

```json
{
  "host": {
    "connectionReferenceName": "shared_sharepointonline",
    "operationId": "GetFileMetadataByPath"
  },
  "parameters": {
    "dataset": "https://erfxxam.sharepoint.com/sites/O365_msteams_5b3dce",
    "path": null
  }
}
```

실패 표시 없이 6분 이상 대기했다. 원인은 메타데이터 단계에 전달되는 파일 경로가 `null`인 것으로 판단된다. Flow가 멈춘 뒤 재시작해도 정상화되지 않았으며 후속 진단이 필요하다.

### Flow 매개변수 기록 충돌

대화 기록에 서로 다른 `.Run(...)` 시그니처가 존재한다.

- 최근 작업 기록 A: `number, text, text_1, text_2, text_3`
- 이전 시작문 기록 B: `number, text, text_1, text_2, text_3, number_1, text_4, text_5`

다음 대화에서는 Power Apps 데이터 패널의 실제 Flow 연결과 현재 `Run` 호출 도움말을 먼저 캡처하여 확정한다.

### 기타 주의

- `txt시료명`의 길이 문제처럼 보였던 현상은 공백 제거 후 해결됨
- Office Script 이름을 영문으로 바꾸는 작업은 연결 문제 우려가 있어 임의 변경하지 않음
- 화면의 노란 경고는 기능 검증 후 한꺼번에 점검
- 트리 정리는 기능이 모두 검증된 뒤 수행

### 2026-08-05 구분변경 Excel 잠금 및 재제출 — 원인 확인, 안정화 보류

- 첫 실패 작업: `기존폴더이동및이름변경`
- SharePoint 오류: 내부 Excel이 연결 계정에 의해 shared use로 잠겨 폴더 이동 거부
- 사람이 Excel·Teams 폴더를 열어 둔 상태는 아니었음
- 대기 후 Flow 실행 기록 재제출 시 실제 폴더·파일 변경 성공
- 재제출은 Power Apps를 다시 실행하지 않으므로 응답 기반 접수헤더 Patch는 실행되지 않음
- 동일 건에서 Power Apps의 `btn구분변경실행`을 다시 누르면 새 번호와 변경이력이 중복될 수 있으므로 금지
- 향후 Flow 내부 SharePoint 업데이트 및 잠금 조건부 재시도 구조로 개선

## 6. 과거 작업 우선순위 및 진행 기록

### 2026-08-10 세션 종료 체크포인트

- 오늘 `scr분석진행`에서 조회 갤러리, 개별·현재 조회 전체선택, 선택 건수, 분석완료일 지정, 선택 항목 분석완료 처리와 SharePoint 반영을 검증했다.
- 의뢰번호 직접 검색안은 폐기하고 업체/현장·의뢰일자 월·분석항목 조합 필터를 구현했다.
- 교차 목록 `LookUp(접수헤더, ...)`의 위임 경고를 운영 전 안정화하기로 결정하고 SharePoint `접수항목상세`에 복사 열 5개를 생성했다.
- `formulas/scr검토저장_btn최종저장_OnSelect.powerfx`의 상세 생성 분기 3곳에 복사 열 저장을 추가했다.
- 신규 테스트에서 복사 열은 생성됐으나 `접수항목상세.접수헤더연결`이 비어 저장되는 오류를 확인했다.
- 저장소 최신 교정본은 `접수항목상세`와 `접수시료항목매핑`의 Lookup 저장 7곳을 `{ Id: var저장헤더.ID, Value: var저장헤더.제목 }` 직접 레코드 방식으로 변경했다. 이 교정은 아직 Power Apps 신규 저장으로 검증하지 않았다.
- 저장소 파일 검증 결과 직접 `접수헤더연결` 레코드는 7곳, 복사 열 5종은 각각 3곳이며 전체 PowerFx는 중복 없는 단일 수식이다.
- 개인 PC Local Codex가 하루 업무 종료 시 같은 대화의 확정 결과를 `C:\workspace\TAT`에 반영하고 검증·커밋·`origin/main` push를 수행하는 운영 방식을 사용한다.
- 회사 PC에서는 하루 동안 Power Apps·SharePoint·Power Automate 작업과 검증을 계속하며, 사소한 작업마다 Local Codex로 전환하지 않고 세션 종료 시 한 번에 Git 체크포인트를 만든다.
- 다음 대화는 GitHub `main`의 최신 문서와 수식을 가져와 세션 시작 게이트를 통과한 뒤 아래 재개 위치부터 시작한다.

### 다음 세션 재개 위치

1. `scr검토저장 > btn최종저장.OnSelect`에 GitHub 최신 전체 수식을 적용하고 Power Apps 문법 오류를 확인한다.
2. 신규 테스트 한 건으로 `접수항목상세.접수헤더연결`과 `접수시료항목매핑.접수헤더연결`이 같은 신규 헤더를 가리키는지 확인한다.
3. 신규 상세 행의 복사 열 5개와 결과 폴더·Excel 생성을 함께 확인한다.
4. 성공하면 직접 Lookup 레코드 방식을 검증 완료로 기록하고 실패 테스트 행 ID 82·83의 복구 또는 삭제를 결정한다.
5. 이어서 숫자 열 `접수헤더ID복사` 추가·인덱싱 및 `RemoveIf` 직접 숫자 비교 전환을 진행한다.
6. 기존 데이터 일괄 보정 후에만 `gal분석진행항목.Items`와 시료명요약 표시를 복사 열 직접 참조 방식으로 교체한다.

1. 신규 저장에서 비어 버린 `접수헤더연결`을 직접 Lookup 레코드 저장 방식으로 교정한 전체 수식을 Power Apps에 적용하고 새 테스트 건으로 검증한다.
2. `RemoveIf(..., 접수헤더연결.Id = ...)` 위임 경고를 없애기 위해 숫자 복사 열 `접수헤더ID복사` 추가 여부와 적용 범위를 확정한다.
3. 현재 실패 테스트 행 ID 82·83의 `접수헤더연결`을 복구하거나 검증용 데이터이면 삭제 후 재생성한다.
4. 기존 `접수항목상세`를 Power Automate 페이지 처리로 일괄 보정한다.
5. `gal분석진행항목.Items`와 시료명요약 레이블을 복사 열 직접 참조 방식으로 교체하고 위임 경고를 재확인한다.
6. 필터 초기화와 단독·조합 필터, 전체선택, 분석완료 처리 범위를 다시 검증한다.
7. 구분변경 안정화 작업은 위 신규 기능 이후 작업으로 계속 보류한다.
8. 현재 테스트 건 `ENE-260805-016`은 실제 파일과 접수헤더 경로 4개가 불일치하므로 운영 데이터로 사용하기 전에 수동 보정하거나 안정화 작업 시 복구 대상으로 남긴다.
9. 향후 안정화 시 Flow 응답 4개 수식 교정 → Flow 내부 접수헤더·변경이력 업데이트 → Excel 잠금 조건부 재시도 순으로 진행한다.
10. 안정화 후 새 테스트 건으로 정상 호출과 실행 기록 재제출 두 경로를 각각 검증한다.

### 2026-08-06 신규 기능 요구사항 — 구현 진행

- 목적: 항목별 의뢰번호·시료명·수량을 한 화면에서 간단히 확인하고 여러 항목을 한 번에 분석완료 처리한다.
- 조회: 각 행에 의뢰번호, 시료명요약, 분석항목, 수량을 표시한다.
- 선택: 화면 상단의 전체선택 또는 선택 제어를 통해 처리할 항목을 체크한다.
- 처리: 화면 상단에서 날짜를 지정한 뒤 실행 버튼을 누르면 선택 항목의 분석상태와 분석완료일을 일괄 변경한다.
- 행 기준: 기존 `의뢰번호 × 시료명 × 분석항목` 안을 폐기하고 `의뢰번호 × 분석항목` 한 행으로 변경 확정.
- 시료 표시: 시료별 행을 만들지 않고 `scr접수기본정보`에서 입력해 `접수헤더.'시료명 요약'`에 저장한 값을 각 분석항목 행에 표시한다.
- 기대 효과: 시료 수와 분석항목 수를 곱한 대량 행 생성을 피하고, 한 의뢰번호의 행 수를 분석항목 수 수준으로 유지한다.
- 재처리 정책: 이미 `분석완료`인 항목도 재분석 또는 오선택 정정을 위해 다시 선택 가능하며, 실행 시 지정 날짜로 완료일을 갱신한다.
- 사용자 제공 SharePoint 열 화면 확인 결과:
  - `접수항목상세`: `접수헤더연결` Lookup, `분석항목` Lookup, `수량`, `분석상태`, `분석완료일`, `배정시료명` 등을 사용한다.
  - `접수시료항목매핑`: `접수헤더연결` Lookup, `시료순번`, `시료명`, `분석항목연결` Lookup, `분석항목명복사`를 사용한다.
- 현재 상태: 목록과 표시 열 및 행 기준 확인 완료. 실제 Power Apps 화면·트리·컨트롤명과 Lookup의 PowerFx 레코드 참조 형식은 아직 미확정이다.
- Power Apps 화면 확인: `scr분석진행`은 하위 트리가 전혀 없는 빈 화면이다. 신규 화면을 추가하지 않고 이 화면에 분석완료 일괄 처리 UI를 구성한다.
- 사용자 생성 확인: `scr분석진행 > con분석진행전체` 아래에 `con분석진행헤더`, `con분석진행도구`, `con분석진행목록`을 생성했다.
- 사용자 적용 확인: 화면 여백과 내부 자동 레이아웃 크기를 조정했고 헤더·도구·목록 영역이 화면 너비와 높이에 맞게 정상 표시된다.
- 사용자 생성 확인: `lbl분석진행제목`, `con분석진행목록헤더`, `gal분석진행항목`을 추가했다.
- `gal분석진행항목.Items`에 `접수항목상세` ID 내림차순 정렬 수식을 적용했고 실제 행과 스크롤 표시가 정상이다.
- 다음 재개 위치: 갤러리 기본 이미지·화살표·기본 레이블을 정리하고 `의뢰번호`, `시료명요약`, `분석항목`, `수량` 열 표시를 구성한다.
- 구현 순서: 조회 데이터 조합 확정 → 조회 갤러리 → 선택 기능 → 날짜·실행 버튼 → 예외 처리 → 사용자 검증 → 04 문서에 최신 전체 수식 기록.
- 수량 표시: 각 `접수항목상세` 행의 `수량`을 그대로 표시한다.
- 빈 값 정책: `접수헤더.'시료명 요약'` 값이 없으면 대체 문구 없이 공백으로 표시한다.
- 시료명 요약 표시 오류: `LookUp(..., 시료명요약)`은 실제 PowerFx 열명과 달라 인수 오류가 발생했다. 저장소의 최신 최종저장 수식에서 실제 열명이 `'시료명 요약'`임을 재확인해 따옴표를 포함한 열명으로 교정했다.
- 사용자 재검증 결과 `'시료명 요약'`으로 교정한 Lookup 수식이 정상 표시된다.
- 갤러리의 의뢰번호·시료명요약·분석항목·수량 값은 모두 표시되지만 네 레이블의 위치와 크기가 겹쳐 보이므로 다음 작업에서 열별 `X`, `Width`, `Y`, `Height`를 정렬한다.
- 사용자 적용 결과 `gal분석진행항목.TemplateSize = 56`과 네 업무 레이블의 반응형 `X`, `Width`, `Y`, `Height` 정렬이 정상이며 각 행이 한 줄로 겹침 없이 표시된다.
- 다음 재개 위치: 목록 헤더에 `선택 / 의뢰번호 / 시료명요약 / 분석항목 / 수량` 열 제목을 같은 비율로 배치하고 기본 왼쪽 아이콘을 행 선택 체크박스로 교체한다.
- 사용자 적용 확인: `con분석진행목록헤더`에 `선택 / 의뢰번호 / 시료명요약 / 분석항목 / 수량` 열 제목을 갤러리와 같은 너비 비율로 배치했다.
- 다음 재개 위치: 갤러리 기본 왼쪽 아이콘을 삭제하고 `chk분석진행항목선택`과 선택 컬렉션을 연결한다.
- 사용자 검증 결과 `chk분석진행항목선택`의 체크·해제와 `col분석진행선택` 추가·제거 및 스크롤 후 선택 유지 기능은 정상이다.
- UI 보정 필요: 행 체크박스가 선택 열 중앙에서 치우쳐 보이고 목록 헤더 레이블과 갤러리 레이블의 열 시작 위치가 소폭 어긋난다.
- 다음 재개 위치: 체크박스 영역을 48px 전체 폭으로 맞추고 목록 헤더·갤러리의 패딩·간격·고정 열 너비를 동일하게 보정한다.
- 사용자 적용 결과 헤더 컨테이너 간격·패딩, 헤더·행 레이블 패딩, 수량 열 고정 폭, 갤러리 `TemplatePadding` 보정 후 상·하단 열 위치가 정상 정렬됐다.
- `chk분석진행항목선택`은 48px 선택 열 안에서 `CheckboxSize = 36`, 좌우 패딩 각 `6`으로 조정한 사용자 적용값이 정상이다.
- 다음 재개 위치: `con분석진행도구`에 전체선택 체크박스와 선택 건수 레이블을 추가한다.
- 사용자 검증 결과 `lbl분석진행선택건수`가 선택·해제 및 스크롤 후에도 `선택 N건`을 정상 유지한다.
- 2026-08-07 세션 종료 체크포인트: 조회 갤러리, 업무 열 표시, 헤더 정렬, 개별 선택, 선택 건수까지 완료. 전체선택 범위 결정과 전체선택 체크박스, 날짜 선택기, 분석완료 실행 버튼은 다음 주에 이어서 진행한다.
- 2026-08-10 재개 결정: 전체선택 대상은 SharePoint 전체 원본이 아니라 `scr분석진행`의 현재 조회 결과로 확정했다. 현재 갤러리에는 별도 필터가 없으므로 우선 `gal분석진행항목.AllItems`에 현재 로드된 행을 대상으로 구현하고, 향후 조회 필터 추가 시 같은 범위를 유지한다.
- 사용자 검증 결과 `chk분석진행전체선택`의 전체선택·전체해제, 개별 선택과 상단 체크 상태 동기화, 선택 건수, 스크롤 후 상태 유지가 모두 정상이다.
- 다음 재개 위치: `con분석진행도구`에 분석완료일 레이블과 날짜 선택기를 추가한다.
- 사용자 생성·검증 완료: `con분석진행도구 > lbl분석진행완료일`, `dp분석일`을 추가했고 오늘 날짜 기본 표시, 날짜 변경·유지, 기존 선택 기능이 모두 정상이다.
- 다음 재개 위치: SharePoint `접수항목상세.분석상태` Choice 목록에서 실제 완료값을 확인한 뒤 `btn분석진행완료처리.OnSelect` 전체 수식을 작성한다.
- 사용자 제공 SharePoint 열 설정으로 `접수항목상세.분석상태`의 실제 Choice 값을 `대기 / 분석 중 / 분석완료 / 보류`로 확인했다.
- 다음 재개 위치: `con분석진행도구 > btn분석진행완료처리`를 추가하고 선택 행만 `분석완료` 및 지정 날짜로 Patch한다.
- 사용자 실행 결과 `btn분석진행완료처리`가 선택 행의 `분석상태 = 분석완료`, `분석완료일 = dp분석일.SelectedDate`를 SharePoint `접수항목상세`에 정상 반영했다.
- 다음 기능 요구: `scr분석진행`에 의뢰번호 검색과 분석항목 필터를 추가한다.
- 요구사항 변경: `txt분석진행의뢰번호검색`은 생성하지 않았다. 사용자가 의뢰번호를 기억해 직접 입력하기 어렵기 때문에 해당 텍스트 검색안은 폐기한다.
- 대체 필터 제안: 업체/현장, 월, 분석항목을 조합해 같은 업체의 서로 다른 입고월을 구분한다.
- 사용자 확인: 업무상 입고월과 `접수헤더.의뢰일자`는 동일하다.
- 사용자 생성 확인: `cmb분석진행분석항목필터`를 생성했다. 실제 필터 동작은 업체/현장·월 필터와 갤러리 수식 조합 후 검증한다.
- 다음 재개 위치: `cmb분석진행업체필터`, `dd분석진행월필터`를 생성하고 세 필터를 `gal분석진행항목.Items`에 조합한다.
- 사용자 생성 확인: `con분석진행필터`에 `cmb분석진행업체필터`, `dd분석진행월필터`, `cmb분석진행분석항목필터`를 구성했고 각 선택지가 표시된다.
- UI 보정 필요: 월 드롭다운과 두 콤보 상자의 높이가 다르고 필터 영역이 도구 영역 아래에 배치돼 있다.
- 다음 재개 위치: `con분석진행필터`를 `con분석진행도구` 위로 이동하고 세 필터의 높이·수직 정렬을 통일한 뒤 갤러리 필터 수식을 적용한다.
- 사용자 적용 확인: `con분석진행필터`를 `con분석진행도구` 위로 이동했고 세 필터의 `AlignInContainer = AlignInContainer.Center` 적용만으로 높이와 수직 정렬이 통일됐다.
- 다음 재개 위치: 업체/현장·의뢰일자 월·분석항목 조건을 `gal분석진행항목.Items`에 조합한다.
- 사용자 구현 확인: `gal분석진행항목.Items`에 업체/현장 Lookup, `접수헤더.의뢰일자` 월, 분석항목 Lookup 조건을 조합한 필터 수식을 적용했다.
- 현재 상태: 필터 수식 적용 및 화면 동작 확인. 각 단독·조합 조건과 필터 밖 항목 보호는 추가 사용자 검증 대상으로 남긴다.
- 다음 재개 위치: 필터 행의 기존 `버튼`을 `btn분석진행필터초기화`로 정리하고 세 필터·선택 컬렉션을 동시에 초기화한다.
- 사용자 확인: 현재 조합 필터를 포함해 SharePoint 원본을 사용하는 여러 Power Apps 수식에 위임 경고가 표시된다.
- 현재 필터의 직접 원인: `접수항목상세` 각 행에서 `LookUp(접수헤더, ...)`를 실행해 업체/현장과 의뢰일자를 비교하는 교차 목록 조회는 SharePoint에 위임되지 않는다.
- 위험: 데이터 행 제한을 넘으면 오래된 월 또는 일부 업체의 항목이 갤러리·전체선택·분석완료 처리 대상에서 누락될 수 있다.
- 안정화 제안/미적용: `접수항목상세`에 의뢰일자와 업체/현장 ID·표시값을 복사 저장해 같은 목록의 직접 열로 필터링하고 기존 데이터도 보정한다.
- 사용자 결정: 현재 경고를 임시 허용하지 않고 복사 열 추가·신규 저장 수식 보완·기존 데이터 보정을 진행한다.
- 다음 재개 위치: SharePoint `접수항목상세`에 복사 열 5개를 정확한 형식으로 생성한다.
- 사용자 생성 완료: `의뢰일자복사`, `업체현장ID복사`, `업체현장명복사`, `시료명요약복사`, `분석항목ID복사`를 SharePoint `접수항목상세`에 생성했다.
- 저장소 수식 보완: `formulas/scr검토저장_btn최종저장_OnSelect.powerfx`의 `접수항목상세` 생성 분기 3곳에 복사 열 저장을 추가했다.
- 사용자 적용 결과: 신규 상세 행과 복사 열 값은 생성됐으나, 테스트 행 ID 82·83의 필수 Lookup `접수헤더연결`이 비어 저장됐다.
- 원인 판단: 신규 헤더를 만든 직후 `LookUp(Choices(...접수헤더연결), Id = var저장헤더.ID)`로 다시 찾는 방식이 새 헤더 Lookup 레코드를 반환하지 못해 `Blank()`가 저장된 것으로 본다.
- 저장소 교정안(미검증): 상세와 시료-항목 매핑 생성 분기의 `접수헤더연결`을 `Choices` 재조회 대신 `{ Id: var저장헤더.ID, Value: var저장헤더.제목 }` 직접 레코드로 저장하도록 전체 수식을 갱신했다.
- 별도 잔여 오류: `RemoveIf(접수항목상세, 접수헤더연결.Id = var저장헤더.ID)`에는 SharePoint Lookup 하위 `Id` 비교 위임 경고가 남아 있다. 기존 복사 열 5개에는 헤더 ID가 없으므로 이 경고는 해결되지 않는다.
- 제안/미적용: `접수항목상세`에 숫자 열 `접수헤더ID복사`를 추가·인덱싱하고 신규 저장·삭제 조건을 이 직접 숫자 열로 전환한다.
- 현재 재개 위치: 교정된 전체 수식의 Power Apps 문법을 먼저 확인한 뒤 신규 한 건으로 `접수헤더연결` 저장을 검증하고, 이어서 `접수헤더ID복사` 열 추가를 결정한다.

## 7. 향후 구분변경 안정화 재개 시 필요한 자료

- 변경 전·후 접수상세 화면
- 변경 후 실제 결과 폴더명과 Excel 파일명
- 변경 후 Excel B4의 접수번호
- SharePoint `접수시료항목매핑`의 제목 및 `접수헤더연결`
- `접수헤더.폴더처리상태`
- `접수구분변경이력.처리결과`
- 실패 시 Flow의 빨간 작업 입력·출력

## 8. 2026-08-12 시료 구성 수정 검증 완료

- `btn접수내용수정`은 수정 대상의 기본정보·분석항목·시료 배정을 불러와 `scr접수기본정보`로 이동한다.
- 시료수와 줄바꿈/쉼표로 분리한 시료명 개수 불일치 경고, 기존 배정 유지, 추가 시료 미선택, 제거 시료 배정 삭제를 검증했다.
- `ENE-260811-018` 시료 `1→2` 증가와 `2→1` 감소 모두 접수헤더, 상세, 복사 열 5개, 시료-항목 매핑, 기존 Excel 재작성을 성공했다.
- Flow에 `수정` 분기를 추가해 기존 Excel 메타데이터 조회·`TAT_결과파일작성` 실행·Power Apps 응답·성공 종료를 처리하고 구분변경 작업은 건너뛰도록 검증했다.
- 수정 Flow의 기존 경로 4개는 `var상세접수`가 아니라 `LookUp(접수헤더, ID = var저장헤더.ID)`로 직접 조회해 전달하며, 실제 Power Apps 호출에서 경로 4개·Flow·Excel·`처리완료` 반영을 확인했다.
- 후속 완료: `접수헤더ID복사` 숫자 열·인덱스와 `RemoveIf` 직접 숫자 비교를 적용해 위임 경고 제거 및 신규 저장을 검증했다.
