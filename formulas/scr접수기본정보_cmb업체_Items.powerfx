Sort(
    ShowColumns(
        AddColumns(
            Filter(
                현장마스터,
                기본현장여부 = true
            ),
            Value,
            Text(업체명복사),
            CompanyKey,
            Text(업체명키)
        ),
        ID,
        Value,
        CompanyKey
    ),
    Value,
    SortOrder.Ascending
)
