# TAT 세션 시작 게이트 — 본 작업보다 먼저 실행

이 저장소는 TAT 프로젝트의 정본이다. TAT 관련 분석, 답변, 수식 작성, 파일 수정 전에 아래 게이트를 통과해야 한다.

## 1. 게이트 실행 시점

아래 경우에만 게이트를 실행한다.

- 새 대화 또는 새 작업 세션을 시작할 때
- 대화가 길어져 컨텍스트가 요약·초기화됐을 때
- 원격 `main`이 세션 기준 커밋 이후 변경됐을 때
- 문서와 실제 Power Apps·SharePoint·Power Automate 상태가 충돌할 때
- 사용자가 최신 Git 기준 재확인을 요청했을 때

같은 세션에서 작업 A, B, C로 넘어갈 때마다 반복 실행하지 않는다.

## 2. 필수 실행 순서

1. 첫 명령으로 `bash scripts/tat-preflight`를 실행한다.
2. 원격 저장소가 `beartaesu/TAT`, 기준 브랜치가 `main`인지 확인한다.
3. `AGENTS.md`와 아래 문서를 순서대로 전부 읽는다.
   1. `docs/01_TAT_MASTER.md`
   2. `docs/02_TAT_CURRENT_STATUS.md`
   3. `docs/03_TAT_DECISIONS.md`
   4. `docs/04_TAT_FORMULAS_ERRORS_HANDOFF.md`
4. 최근 실행 결과가 제공됐다면 문서 기록과 대조한다.
5. 아래 시작 확인 블록을 사용자에게 출력한 뒤에만 본 작업을 시작한다.

```text
[TAT 작업 시작 확인]

저장소: beartaesu/TAT
브랜치: main
기준 커밋: <SHA>
확인 완료:
- AGENTS.md
- 01_TAT_MASTER.md
- 02_TAT_CURRENT_STATUS.md
- 03_TAT_DECISIONS.md
- 04_TAT_FORMULAS_ERRORS_HANDOFF.md

현재 적용 상태: <짧은 요약>
이번 세션 작업: <이어갈 작업>
문서 충돌/확인 필요: <없음 또는 항목>
```

시작 확인 블록 전에는 Power Apps 수식 제공, Power Automate 수정 안내, 기존 구조 판단, 파일 수정, Git 반영을 하지 않는다. 게이트가 실패하면 실패 원인을 먼저 보고하고 정본을 추측하지 않는다.

## 3. 정본 우선순위

1. `beartaesu/TAT main`의 최신 파일
2. 사용자가 제공한 최신 실제 화면·수식·실행 로그
3. 현재 대화의 확인된 결과
4. 대화 요약·기억

문서끼리 충돌하거나 문서와 실제 시스템이 다르면 임의로 합치지 않는다. 차이를 명시하고 사용자 확인 또는 실제 화면·로그 확인 후 갱신한다.

# 문서 역할과 갱신 기준

| 문서 | 역할 | 포함 | 제외 |
|---|---|---|---|
| `01_TAT_MASTER.md` | 장기 통합 설계 기준 | 시스템 구조, 데이터 모델, 화면·Flow 큰 흐름, 확정 운영 원칙 | 일회성 오류 로그, 미검증 수식 원문 |
| `02_TAT_CURRENT_STATUS.md` | 현재 작업대장 | 구현·검증 상태, 열린 오류, 재개 위치, 다음 우선순위 | 장기 설계 반복, 전체 수식 원문 |
| `03_TAT_DECISIONS.md` | 확정 결정 기록 | 채택·폐기·보류 결정, 금지사항, 결정 근거 | 진행 중 추측, 상세 오류 로그 |
| `04_TAT_FORMULAS_ERRORS_HANDOFF.md` | 실행 가능한 인계 자료 | 최신 전체 수식, 정확한 위치, Flow 입출력, 오류와 해결, 구버전 보관 | 최신/구버전 표시 없는 수식, 근거 없는 추정 |

갱신은 모든 문서를 기계적으로 고치는 방식이 아니라 변경 성격에 맞는 문서만 갱신한다.

- 설계·목록·화면·Flow의 장기 기준 변경 → 01
- 구현·검증 상태, 오류 상태, 다음 작업 변경 → 02
- 채택·폐기·보류·금지 결정 확정 → 03
- 수식·Flow 표현식·오류 로그·인계 절차 변경 → 04
- 하나의 결과가 여러 역할에 해당할 때만 여러 문서를 함께 갱신한다.
- 각 갱신에는 날짜, 검증 상태, 현재 재개 위치를 필요한 범위에서 반영한다.
- 미검증 내용은 `미검증`, 향후안은 `제안/보류`, 폐기 수식은 `구버전·적용 금지`로 표시한다.

# 작업 중 필수 원칙

- Power Apps 수식은 정확한 화면, 트리, 컨트롤, 속성을 먼저 명시한다.
- 변경 부분만 제시하지 않고 기존 속성을 통째로 교체할 수 있는 전체 수식을 제공한다.
- 컨트롤명, SharePoint 열명, Choice 값, Lookup 구조, Flow 매개변수 이름·형식·순서를 추측하지 않는다.
- 한 작업 단위는 확인 → 변경안 제공 → 사용자 테스트 → 결과 기록으로 처리한다.
- 같은 세션에서는 기준 커밋과 확인한 문맥을 유지하며 여러 작업을 연속 처리한다.

# Git 반영 단일 규칙

이 절이 TAT 문서의 Git 반영 기준이다. 다른 문서에 다른 표현이 있으면 이 절을 우선하고 충돌 문구를 수정한다.

- 작업 중 확인 결과는 관련 MD에 누적한다.
- 다음 체크포인트에서 관련 변경을 묶어 커밋한다.
  - 기능 하나가 실제로 검증 완료됐을 때
  - 중요 오류의 원인과 해결·보류가 확정됐을 때
  - 세션을 종료하거나 새 대화로 넘기기 전
  - 사용자가 즉시 커밋을 요청했을 때
- 사소한 안내 하나마다 커밋하지 않는다.
- 사용자가 별도 브랜치나 PR을 요청하지 않은 TAT 문서 체크포인트는 `beartaesu/TAT main`에 반영한다.
- 관련 없는 변경은 포함하지 않는다.
- 반영 후 커밋 SHA와 변경 파일을 확인해 사용자에게 보고한다.
